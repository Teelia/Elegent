# 问题修复快速参考

> 基于 Task 74 错误分析的48条错误结论修复指南

---

## 问题分类速查表

| 问题类型 | 典型特征 | 影响行号示例 | 修复方法 |
|---------|---------|-------------|---------|
| **格式错误** | 17位、19位身份证号 | 0, 144 | L1: 严格18位验证 |
| **当事人遗漏** | 提及但未标注身份信息 | 9, 52, 95 | L2+L3: 扩大识别范围 |
| **信息缺失** | "拒绝"、"未提供"、"无果" | 707, 多行 | L2+L3: 关键词检测 |
| **豁免误用** | 撤警关键词覆盖检查 | 部分 | L3: 明确豁免条件 |

---

## 关键代码修改位置

### 文件1: `BuiltinGlobalLabelsInitializer.java`

```java
// ===== 修改点1: 预处理配置 (约197-232行) =====
private String buildPolicePersonnelPreprocessorConfigJson() {
    // ...
    policy.put("id15IsValid", false); // ✅ 改为false
    policy.put("idNonStandardIsInvalid", true); // ✅ 新增
    // ...
}

// ===== 修改点2: 标签描述 (约270-280行) =====
private String buildPolicePersonnelLabelDescription() {
    return "..." // 使用优化方案文档中的完整版本
}

// ===== 修改点3: 二次强化模板 (约294-333行) =====
private String buildPolicePersonnelEnhancementTemplate() {
    return "..." // 使用优化方案文档中的完整版本
}

// ===== 修改点4: 触发阈值 (约70行) =====
buildEnhancementConfigJson(60, policeEnhancementPromptId), // ✅ 75→60
```

### 文件2: `EnhancementConfig.java`

```java
// ===== 新增方法: 扩展触发条件 =====
public boolean shouldTrigger(int initialConfidence, String validationResult) {
    // 原有逻辑 + 新增强制触发条件
    // 详见优化方案文档 3.4节
}
```

### 文件3: 新增文件

- `IdCardLengthValidator.java` - 身份证号严格验证器
- `PostProcessValidator.java` - 后置规则验证器

---

## 测试验证命令

### 1. 本地测试

```bash
# 启动应用
cd backend
mvn spring-boot:run

# 查看日志
tail -f logs/application.log | grep -E "初始化|二次验证|后置验证"
```

### 2. 接口测试

```bash
# 重新分析测试数据
curl -X POST http://localhost:8080/api/tasks/74/re-analyze \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 查看结果
curl http://localhost:8080/api/tasks/74/results \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 3. 结果验证

```bash
# 使用Python脚本对比结果
python test_optimization.py
```

---

## 预期效果对比

### 修复前 (v1)

| 行号 | 问题 | 判定 | 原因 |
|-----|------|------|------|
| 0 | 19位身份证号 | 是 ❌ | 长度未严格验证 |
| 9 | 小孩信息缺失 | 是 ❌ | 当事人识别不全 |
| 52 | 中介信息缺失 | 是 ❌ | 当事人识别不全 |
| 144 | 17位身份证号 | 是 ❌ | 长度未严格验证 |
| 707 | 拒绝提供信息 | 是 ❌ | 关键词未识别 |

### 修复后 (v2)

| 行号 | 问题 | 判定 | 原因 |
|-----|------|------|------|
| 0 | 19位身份证号 | 否 ✅ | L1检测到格式错误 |
| 9 | 小孩信息缺失 | 否 ✅ | L2+L3识别隐含当事人 |
| 52 | 中介信息缺失 | 否 ✅ | L2+L3识别隐含当事人 |
| 144 | 17位身份证号 | 否 ✅ | L1检测到格式错误 |
| 707 | 拒绝提供信息 | 否 ✅ | L2+L3检测缺失关键词 |

---

## 快速决策树

```
收到一条警情数据
    ↓
【L1规则】提取所有身份证号
    ↓
  存在非18位？ ──是→ 判定为"否" ──┐
    ↓ 否                         │
【L2预处理】检测信息缺失关键词      │
    ↓                           │
  存在"拒绝/未提供/无果"？ ──是→ 判定为"否" ─┤
    ↓ 否                        │              │
【L2预处理】识别所有涉警当事人      │              │
    ↓                           │              │
  所有人都有身份信息？ ──否→ 判定为"否" ─┤      │
    ↓ 是                        │      │      │
【LLM判断】调用大模型              │      │      │
    ↓                           │      │      │
  置信度<60？ ──是→ 【L4二次强化】 │      │      │
    ↓ 否                        │      │      │
【L5后置验证】规则层面最终检查      │      │      │
    ↓                           │      │      │
  验证通过？ ──否→ 修正为"否" ────┴──────┘      │
    ↓ 是                                      │
  输出: "是" ◄────────────────────────────────┘
```

---

## 版本对比摘要

| 特性 | v1 (当前) | v2 (优化) |
|-----|----------|----------|
| 身份证号长度验证 | 宽松(允许15位) | 严格(仅18位) |
| 当事人识别范围 | 明确标注 | 含隐含推断 |
| 信息缺失检测 | 无 | 有(关键词+模式) |
| 二次强化阈值 | 75 | 60 |
| 强制触发条件 | 无 | 有(格式错误/缺失) |
| 后置验证 | 无 | 有(规则引擎) |
| 预期准确率 | ~93% | ≥98% |

---

## 紧急修复（仅修改核心问题）

如果时间紧张，**最小修改集**：

### 修改1: 预处理配置（5分钟）

```java
// BuiltinGlobalLabelsInitializer.java:204-209
policy.put("id15IsValid", false); // 改这1行
```

### 修改2: 标签描述（10分钟）

在描述中增加：
```java
+ "✗ 身份证号非18位（17位、19位、15位等均不合格）\n"
+ "✗ 出现\"拒绝提供\"、\"未登记\"、\"研判无果\"等明确表示信息缺失的关键词\n"
```

### 修改3: 触发阈值（1分钟）

```java
// BuiltinGlobalLabelsInitializer.java:70
buildEnhancementConfigJson(60, ...) // 75改为60
```

**预期效果**: 修复约60%的错误（格式错误 + 关键词未识别）

---

## 完整修复时间估算

| 修复范围 | 修改文件 | 工作量 | 预期修复率 |
|---------|---------|--------|-----------|
| 紧急修复 | 1处 | 15分钟 | ~60% |
| 标准修复 | 3处 | 2小时 | ~85% |
| 完整修复 | 5处+新文件 | 1天 | ~95% |

**建议**: 先执行紧急修复验证效果，再决定是否继续完整修复

---

## 联系与支持

- **优化方案文档**: `docs/优化方案-涉警当事人信息完整性检查.md`
- **实施指南**: `docs/实施指南-分步执行.md`
- **代码文件**: `backend/src/main/java/com/datalabeling/`

---

**版本**: v1.0
**更新**: 2026-01-28
**状态**: 待实施
