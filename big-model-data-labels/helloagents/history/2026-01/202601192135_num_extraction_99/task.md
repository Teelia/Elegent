# 任务清单: 号码类标签提取与分析链路 99% 准确改造

目录: `helloagents/history/2026-01/202601192135_num_extraction_99/`

---

## 1. 基线评估与口径统一
- [√] 1.1 明确业务口径：15位旧身份证不计入“不满足18位的错误身份证号”（Task43），形成可测试的验收标准
  > 备注: 口径确认（2026-01-19）：15位（一代身份证）视为正确号码，不纳入 Task43 输出；已同步更新实现与回归测试
- [√] 1.2 建立回归集与度量脚本：输出 precision/recall/F1、needs_review 比例（避免只看“命中数”）
  > 备注: 已新增 Task43 回归测试（读取仓库内xlsx回归集，避免系统性漏检）

## 2. 号码证据建模（核心）
- [√] 2.1 新增 `NumberEvidenceExtractor`：实现归一化、候选抽取、分类校验、冲突仲裁与派生字段输出
- [√] 2.2 设计并落地证据 JSON 结构（脱敏版/提示词版），对外统一返回/存储格式
- [√] 2.3 增加脱敏工具：日志、提示词、导出统一走同一套脱敏方法
  > 备注: 已提供号码脱敏工具并在关键路径使用；存量日志与导出链路可后续分批统一改造

## 3. 修复 Task43（优先回归）
- [√] 3.1 将否定条件任务（如 Task43）从 `SmartCardIdentifier` 路径迁移到 `NumberEvidenceExtractor`（避免冲突吞噬）
- [√] 3.2 回归测试：对 `analysis-task-43-results-with-reasoning.xlsx` 进行对比评估，确保系统性漏检消失

## 4. 智能识别器冲突策略修正（可选/视现状）
- [-] 4.1 修正“先占范围再验证”的冲突策略：仅对已验证通过/高置信结果占用 range
  > 备注: 本次通过引入“号码证据建模”替代该路径，避免影响面扩大；SmartCardIdentifier 如需保留可后续再重构
- [-] 4.2 或将 `SmartCardIdentifier` 降级为“候选生成器”并不作为 SSOT
  > 备注: 已在提示词建模链路改用证据模型，SmartCardIdentifier 不再作为权威预识别来源

## 5. LLM 提示词与输出约束
- [√] 5.1 改造提示词构建：引入“证据约束”模板，LLM 必须引用证据ID，不得输出证据外号码
  > 备注: 已在自由提取提示词中注入证据列表（含ID）与“禁止输出列表外号码”的约束文案
- [-] 5.2 输出结构标准化：分析结论/提取结果统一 JSON schema，便于二次校验与前端展示
  > 备注: 需结合前端展示与任务结果结构统一规划，建议后续以单独方案包推进

## 6. 一致性校验与二次强化（可选）
- [-] 6.1 新增 `RuleConsistencyValidator`：校验证据引用、结论与派生字段一致性
- [-] 6.2 二次 LLM 复核仅用于歧义样本（needs_review）并可配置开关

## 7. 安全检查
- [√] 7.1 执行安全检查（按G9：输入验证、敏感信息处理、权限控制、避免将PII写入日志/提示词）
  > 备注: 新增链路默认对外展示/提示词使用脱敏号码；并避免在关键日志输出明文号码

## 8. 文档更新
- [√] 8.1 更新知识库：`helloagents/wiki/arch.md`、`helloagents/wiki/data.md`、模块文档（记录证据 JSON 与流程）

## 9. 测试
- [√] 9.1 增加单元测试：覆盖 17/19/22 位身份证结构候选、Luhn 与身份证结构冲突样本
- [-] 9.2 增加端到端回归：Task43 与常用标签（身份证存在/错误、银行卡存在）跑通全链路
  > 备注: 本次优先修复并回归 Task43；常用标签的端到端回归建议在业务口径确认后补齐
